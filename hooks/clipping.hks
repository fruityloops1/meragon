00242540: // remove clipping (a.k.a. pop-in/low render distance)
    mov r0, r5 // make unconditional
    bl al::invalidateClipping(al::LiveActor*) // ""

00242558: // ignore InitClipping.byml 'Radius' key
    mov r0, #0
00242608: // ignore InitClipping.byml 'NearClipDistance' key
    mov r0, #0

002795c0: // make al::LiveActor::startClipped do nothing
    bx lr